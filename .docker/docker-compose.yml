version: '3.4'
services:
  proto_build_ping-grpc-client:
    build: 
      context: proto_build
      target: base
    tty: true
    user: "${_USER}"
    privileged: true
    container_name: proto_build_ping-grpc-client
    volumes:
      - "../proto/:/target_data:ro"
      - "../proto/go:/target_data/go:rw"
  go_build_ping-grpc-client:
    build: go_build
    tty: true
    user: "${_USER}"
    privileged: true
    container_name: go_build_ping-grpc-client
    volumes:
      - "../:/target_data:rw"
